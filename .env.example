# Copy this file to .env and fill in your key. .env is gitignored.

# Required for the suggestion engine (Training Mode and live calls).
# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Required for live-call transcription (not needed for Training Mode).
# Get a key at https://console.deepgram.com/
DEEPGRAM_API_KEY=

# Optional — port the REISift webhook receiver listens on (see the README's
# Property Context section). Defaults to 4790 if unset.
REISIFT_WEBHOOK_PORT=
