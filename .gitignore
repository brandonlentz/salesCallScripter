node_modules/
.env
.env.local
dist/
out/
.DS_Store
*.log

# Real call transcripts and seller data contain customer PII — keep local only.
transcripts/
data/
recordings/

# Compiled native helper — rebuilt locally via `npm run build:audiotap`
# (requires Xcode Command Line Tools, so not something CI/other machines
# can just check out and run).
native/audiotap/build/
